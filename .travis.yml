language: node_js
node_js:
  - 'stable'

script:
  - npm run test
  - npm run build

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $pmpy
  local_dir: ./build
  on:
    branch:
      - 2hw1
